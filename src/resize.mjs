import path from 'path'

import fs from '@magic/fs'
import is from '@magic/types'
import log from '@magic/log'

import parse from '@parametric-svg/parse'

export const resize = async (opts = {}) => {
}

export default resize
